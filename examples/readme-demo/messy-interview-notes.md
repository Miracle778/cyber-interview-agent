# 后端与 Agent 面试随手记

状态机和工作流引擎有什么区别？面试官追问：如果进程中断，任务恢复时怎么保证不会重复写？

Kafka 消费积压排查。先看 lag、消费耗时、分区分配，也要注意下游数据库是不是瓶颈。最好结合 IoT 项目说。

多租户数据隔离有哪些做法，行级隔离和独立库怎么选？

Agent 不是把所有步骤都交给模型。哪些步骤应该代码编排，哪些步骤适合让模型判断？举一个需要 HITL 的例子。

上下文太长怎么办：检索、摘要、offload 各自解决什么问题？摘要以后怎么避免关键事实丢失？

失败的模型调用怎么重试？用户那条消息是否应该再次进入上下文？需要区分 Message 和 Execution。
